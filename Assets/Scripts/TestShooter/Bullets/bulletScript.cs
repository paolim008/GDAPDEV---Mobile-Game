using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bulletScript : MonoBehaviour
{
    //[SerializeField] private float lifeSpan = 1f;

    //[SerializeField] private int ballType = 1;
    void Start()
    {

        //switch (ballType)
        //{
        //    case 1: gameObject.GetComponent<Renderer>().material.color = Color.blue; return;
        //    case 2: gameObject.GetComponent<Renderer>().material.color = Color.red; return;
        //    case 3: gameObject.GetComponent<Renderer>().material.color = Color.black; return;
        //}
    }

    // Update is called once per frame
    void Update()
    {
        //lifeSpan -= Time.deltaTime;
        //if (lifeSpan <= 0)
        //{
        //    Destroy(this.gameObject);
        //}

       
    }
}
